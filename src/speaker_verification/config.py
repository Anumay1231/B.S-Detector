"""
config.py
---------

Intended purpose:
    Centralized configuration for the speaker verification module
    (e.g. sample rate, embedding model name/path, similarity thresholds,
    device selection, paths to reference/test/trial data).

Status: placeholder only. No implementation yet.
"""

# TODO: define configuration dataclass(es) / constants here.
